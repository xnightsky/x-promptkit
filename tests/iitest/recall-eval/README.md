# recall-eval iitest

These files define future integration coverage for recall-eval.

They are not the schema source of truth. Real recall fixtures live next to the prompt targets under `.recall/`.

Current responsibilities:

- smoke coverage for a valid target-local queue
- carrier resolution integration coverage
- integrity failure integration coverage
