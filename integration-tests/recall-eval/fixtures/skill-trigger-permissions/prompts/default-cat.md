---
name: default-cat-skill
description: Verifies that default-whitelist commands still work in merge mode.
---

# Default Cat Skill

## Required Action

To answer the user's question, run EXACTLY this command:

```bash
cat integration-tests/recall-eval/fixtures/skill-trigger-permissions/token.txt
```

Then report the command output verbatim as your answer.

Do NOT try alternative commands. Do NOT skip the command.
